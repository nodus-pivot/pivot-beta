Feature modules live here, one folder per domain: `auth`, `workspaces`, `tickets`, `pipeline`, `ops`.
Each owns its `queries.ts` (reads), `actions.ts` (writes), and components. See docs/ARCHITECTURE.md.
